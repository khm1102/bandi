<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ac.tukorea.bandi.domain.calendar.mapper.CalendarEventMapper">

    <resultMap id="calendarEventResultMap" type="kr.ac.tukorea.bandi.domain.calendar.model.CalendarEvent">
        <constructor>
            <idArg column="calendar_event_id" javaType="java.lang.Long"/>
            <arg column="team_id" javaType="java.lang.Long"/>
            <arg column="title" javaType="java.lang.String"/>
            <arg column="description" javaType="java.lang.String"/>
            <arg column="start_dttm" javaType="java.time.LocalDateTime"/>
            <arg column="end_dttm" javaType="java.time.LocalDateTime"/>
            <arg column="is_all_day" javaType="_boolean"/>
            <arg column="place" javaType="java.lang.String"/>
            <arg column="color_code" javaType="kr.ac.tukorea.bandi.domain.calendar.model.CalendarEventColor"/>
            <arg column="created_by_member_id" javaType="java.lang.Long"/>
            <arg column="updated_by_member_id" javaType="java.lang.Long"/>
            <arg column="created_dttm" javaType="java.time.LocalDateTime"/>
            <arg column="updated_dttm" javaType="java.time.LocalDateTime"/>
            <arg column="deleted_dttm" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <sql id="calendarEventColumns">
        calendar_event_id, team_id, title, description, start_dttm, end_dttm,
        is_all_day, place, color_code, created_by_member_id, updated_by_member_id,
        created_dttm, updated_dttm, deleted_dttm
    </sql>

    <sql id="notDeleted">
        deleted_dttm IS NULL
    </sql>

    <select id="lookupById" resultMap="calendarEventResultMap">
        SELECT <include refid="calendarEventColumns"/>
        FROM calendar_event
        WHERE calendar_event_id = #{calendarEventId}
          AND <include refid="notDeleted"/>
    </select>

    <select id="lookupByIdForUpdate" resultMap="calendarEventResultMap">
        SELECT <include refid="calendarEventColumns"/>
        FROM calendar_event
        WHERE calendar_event_id = #{calendarEventId}
          AND <include refid="notDeleted"/>
        FOR UPDATE
    </select>

    <select id="searchOverlapping" resultMap="calendarEventResultMap">
        SELECT <include refid="calendarEventColumns"/>
        FROM calendar_event
        WHERE start_dttm &lt; #{rangeEnd}
          AND end_dttm &gt; #{rangeStart}
          AND <include refid="notDeleted"/>
        <if test="teamId != null">
          AND (team_id = #{teamId} OR team_id IS NULL)
        </if>
        ORDER BY start_dttm, end_dttm, calendar_event_id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="calendarEventId">
        INSERT INTO calendar_event (
            team_id, title, description, start_dttm, end_dttm, is_all_day, place, color_code,
            created_by_member_id, updated_by_member_id
        ) VALUES (
            #{teamId}, #{title}, #{description}, #{startDttm}, #{endDttm}, #{allDay}, #{place}, #{colorCode},
            #{createdByMemberId}, #{updatedByMemberId}
        )
    </insert>

    <update id="update">
        UPDATE calendar_event
        SET team_id = #{teamId},
            title = #{title},
            description = #{description},
            start_dttm = #{startDttm},
            end_dttm = #{endDttm},
            is_all_day = #{allDay},
            place = #{place},
            color_code = #{colorCode},
            updated_by_member_id = #{updatedByMemberId}
        WHERE calendar_event_id = #{calendarEventId}
          AND <include refid="notDeleted"/>
    </update>

    <update id="delete">
        UPDATE calendar_event
        SET updated_by_member_id = #{updatedByMemberId},
            deleted_dttm = #{deletedDttm}
        WHERE calendar_event_id = #{calendarEventId}
          AND <include refid="notDeleted"/>
    </update>
</mapper>
